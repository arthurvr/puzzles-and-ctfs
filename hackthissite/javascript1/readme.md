# Javascript missions: level 1

The password is simply given in the short source code:

```javascript
function check(x) {
	if (x == "cookies") {
		alert("win!");
		window.location += "?lvl_password="+x;
...
```


