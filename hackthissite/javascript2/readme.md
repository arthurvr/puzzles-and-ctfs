# Javascript missions: level 2

For this mission, we simply have to disable Javascript to gain access. In Firefox this is done on the `about:config` page by changing the `javasript.enabled` setting to `false`.
 
